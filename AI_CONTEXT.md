# AI_CONTEXT.md

# Project Overview

This project is the frontend of a final university assignment for Programming IV.

The backend is already completed, approved and deployed. The frontend must adapt to the existing backend architecture without modifying it unless absolutely necessary.

The objective is not only to finish the application