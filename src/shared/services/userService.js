import _axiosClient from '@/api/axiosClient'

const userService = {
  getProfile: () => {},
  updateProfile: (data) => {},
  getUsers: (_params) => {},
  updateUserRole: (_id, _role) => {},
  getUserById: (id) => {},
  deleteUser: (id) => {},
}

export default userService
