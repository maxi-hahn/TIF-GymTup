import _axiosClient from '@/api/axiosClient'

const classService = {
  getClasses: (_params) => {},
  getClassById: (_id) => {},
  createClass: (data) => {},
  updateClass: (_id, data) => {},
  deleteClass: (_id) => {},
}

export default classService
